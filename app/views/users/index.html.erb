<h1>ユーザー一覧</h1>
<ul class="users">
  <% @users.each do |user| %>
  <li>
    <%= link_to user.name, user_path(user.id) %>
  </li>
  <% end %>
</ul>
<%= paginate @users %>
<%= link_to "Home", books_path %>
<%= link_to "Back", :back %>
